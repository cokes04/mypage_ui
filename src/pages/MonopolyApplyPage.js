import React from 'react';
import { Container } from 'react-bootstrap';

const MonopolyApplyPage = ({novelId}) => {

    return ( 
                <Container>
                    <p>공사중</p>
                </Container>
            );
}

export default MonopolyApplyPage;